<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Fyreplace.Views.Pages.AccountEntriesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fyreplace.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolNegationConverter x:Key="BoolNegationConverter" />
    </Page.Resources>

    <StackPanel
        Width="300"
        Orientation="Vertical"
        Spacing="12">
        <controls:Segmented
            x:Name="Segmented"
            HorizontalAlignment="Stretch"
            IsEnabled="{x:Bind preferences.Account_IsWaitingForRandomCode, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}"
            SelectionMode="Single"
            SelectionChanged="Segmented_SelectionChanged">
            <controls:SegmentedItem
                x:Name="Login"
                x:Uid="AccountEntriesPage_Login" />
            <controls:SegmentedItem
                x:Name="Register"
                x:Uid="AccountEntriesPage_Register" />
        </controls:Segmented>
        <Frame x:Name="Host" />
    </StackPanel>
</Page>
