<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Fyreplace.Views.MainWindow"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fyreplace.Views"
    xmlns:pages="using:Fyreplace.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:viewmodels="using:Fyreplace.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <pages:MainPage x:Name="MainPage" AppWindow="{x:Bind AppWindow}" />
        <TeachingTip
            x:Uid="MainWindow_UserConnectionTip"
            PreferredPlacement="BottomRight"
            IconSource="{ui:FontIconSource Glyph=&#xF427;}"
            IsOpen="{x:Bind viewModel.ShowUserConnectionTip, Mode=TwoWay}">
            <TeachingTip.HeroContent>
                <ProgressBar IsIndeterminate="True" />
            </TeachingTip.HeroContent>
        </TeachingTip>
        <TeachingTip
            x:Uid="MainWindow_EmailVerificationTip"
            PreferredPlacement="BottomRight"
            IconSource="{ui:FontIconSource Glyph=&#xE715;}"
            IsOpen="{x:Bind viewModel.ShowEmailVerificationTip, Mode=TwoWay}">
            <TeachingTip.HeroContent>
                <ProgressBar IsIndeterminate="True" />
            </TeachingTip.HeroContent>
        </TeachingTip>
    </Grid>
</Window>
