<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Fyreplace.Views.MainWindow"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fyreplace.Views"
    xmlns:pages="using:Fyreplace.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d"
    Activated="Root_Activated">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <pages:MainPage x:Name="MainPage" AppWindow="{x:Bind AppWindow}" />
        <TeachingTip
            x:Uid="MainWindow_ConnectionTip"
            PreferredPlacement="BottomRight"
            IconSource="{ui:FontIconSource Glyph=&#xF427;}"
            IsOpen="{x:Bind viewModel.ShowConnectionTip, Mode=TwoWay}">
            <TeachingTip.HeroContent>
                <ProgressBar IsIndeterminate="True" />
            </TeachingTip.HeroContent>
        </TeachingTip>
    </Grid>
</Window>
